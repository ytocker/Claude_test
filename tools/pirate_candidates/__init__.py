"""Scratch builders for the PIRATE costume redesign exploration.

Exploration-only — none of these register in store_skins.BUILDERS or touch
production art. Each design_<N>.py exposes a `build (frame_idx, tilt_deg) ->
Surface` rendered via tools/ninja_render.py.
"""
