"""Scratch candidate builders for the PARROTS rarity-spectrum exploration.

Epic + legendary parrot skins explored via the item-design loop. Each design_N
exposes build(frame_idx, tilt_deg) -> Surface. NOT registered in
store_skins.BUILDERS — production art and the catalog stay untouched until a
winner is picked."""
